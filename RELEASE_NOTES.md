Agent Sphere 0.2.0-13 pins its default install transaction to Core 0.2.0-13 and requires CX-Mesh >= 2.0.0-1. Existing consolidated hosts therefore receive account/path retirement during installation or update. Historical standalone CX packages still require their separately reviewed consolidation first. Version 0.2.0-12 remains immutable component evidence and is not selected for the new aggregate.

Agent Sphere 0.2.0-12 admits the exact CX-Mesh 2.0.0-1 successor after account and path retirement. Repeat installs verify the new paths while retaining historical DPKG ownership checks. Direct legacy predecessor consolidation still requires the reviewed 1.2.0-1 step; the guard does not bypass it.

Agent Sphere 0.2.0-11 supports AGPC's RAM-only Redis release with uChat 3.2.0-2 and uchatd 0.4.0-1. Messages remain durable in SQLite. Before downloads or package changes, and again under APT's lock, the installer rejects legacy or incomplete uchatd installations and orphaned Inbox state. Complete the separately reviewed uChat component migration before retrying an existing installation; no messages are migrated or deleted by this installer. Fresh installations and already-upgraded daemons are admitted. Other component versions and migration permissions are unchanged.

Agent Sphere 0.2.0-9 accepts the exact native CX residual file list containing only the retained obsolete configuration. DPKG produces this form when the shared system directories already belong to other installed packages. The existing directory-bearing form remains accepted. Exact configuration ownership, cleanup hooks, successor identity, file metadata and locked APT checks remain enforced.

Native regression fixtures cover historical CX retirement with pre-existing system directory ownership, the cx-node 0.3.3-6 / cx-mesh 1.1.0-1 residual baseline, guarded upgrade to cx-mesh 1.2.0-1, repeat installation, configuration preservation and rejection of altered residual lists and hooks. The remaining uChat and CX runtime versions are unchanged from 0.2.0-8.

Core now includes mote-mcpd 3.1.0-1, mote-mcp-ultra 0.1.0-1 and cx-loop 0.1.0-4.
The gateway registers with the shared system Codex app/CLI configuration.
CX-Loop contains cx-loopd and depends on uchatd, while retaining disabled
execution defaults and requiring a separately admitted Mesh gateway. No
provider service grant or worker is enabled by this composition change.
